# physics_lab

大学物理实验数据处理工具。针对直接测量法、间接测量法与作图线性拟合，把数据处理过程完整、透明地展示出来，便于华南理工大学（SCUT）的同学快速得到可写进实验报告的结果。

## 功能

### 直接测量法

- 算术平均值与样本标准差（贝塞尔公式）计算
- Grubbs 准则剔除粗大误差，并展示每一步的 G 统计量与临界值对比
- A 类不确定度（`u_A = S / √n`）与 B 类不确定度（`u_B = Δ / √3`，按均匀分布）及合成标准不确定度
- 有效数字修约：不确定度保留 1 位有效数字、只进不舍，测量值末位对齐
- 处理报告可打印到屏幕，也可保存到 `reports/` 文件夹（可选自定义文件名）

### 间接测量法

- 公式解析：支持内置数学/物理常数（`pi`、`c`、`h`、`g_n` 等，运行时会打印常数表）、`^` 幂、隐式乘法（`4pi` ≡ `4*pi`）、`sin/ln/sqrt` 等函数、全角符号自动转换、`π`/`√` 快捷输入
- 变量输入两种模式（对所有变量统一生效）：多次测量原始数据（完整走 Grubbs → uA/uB 流程）或直接输入 均值±不确定度
- 误差传递：一般式 `u_N² = Σ(∂N/∂xᵢ)²uᵢ²`（sympy 符号偏导），幂单项式自动给出教材相对不确定度捷径并对照验证
- 单位贯穿全程：pint 量纲检查（m + s 报错）、推导结果单位、可换算到指定输出单位（如 `cm/s^2`）
- 科学记数法结果：`(9.65 ± 0.05)×10⁴ g` 形式自动处理

### 作图与线性拟合（v0.3.0）

- 最小二乘法作图：按教材公式（`a = Lxy/Lxx`、`b = ȳ−a·x̄`、`R = Lxy/√(Lxx·Lyy)`）拟合并用 matplotlib 绘图，数据点画 "+"、直线标出回归方程与 R，图保存到 `figures/`
- 坐标轴标签支持 `物理量 / 单位` 格式；自动检测系统中文字体（找不到时提示改用英文）
- 求斜率与截距两种方法可选：**图解法**（自动在拟合直线两端取点 A、B 走两点式，另取点 C 求截距）或**线性回归法**（直接给出 a、b、R 与回归方程）
- 斜率自动带单位（如 `Ω/℃`）、截距带纵轴单位；报告展示全部中间量（x̄、ȳ、Lxy、Lxx、Lyy），可保存到 `reports/`

## 安装教程

要求 Python >= 3.10。

```bash
# 1. 克隆仓库
git clone <仓库地址>
cd physics_lab

# 2. 创建并激活虚拟环境
python -m venv .venv
source .venv/bin/activate        # Windows 使用 .venv\Scripts\activate

# 3. 安装依赖与项目本体
pip install -r requirements.txt
pip install -e .
```

不执行`pip install -e .`也可以直接运行（程序会自动找到 `src/` 下的包）

启动程序：
```bash
python main.py
```

## 使用方法

运行后选择处理模式：

```
请选择处理模式：1) 直接测量法  2) 间接测量法  3) 作图与线性拟合: 2
```

### 直接测量法

1. 输入实验数据（空格、逗号、中文逗号、顿号均可分隔），如 `1.0 1.2 1.1 1.0 5.0`
2. 输入仪器误差（B 类不确定度来源），如 `0.01`
3. 程序自动执行 Grubbs 检验（数据少于 3 个时跳过）并展示剔除过程
4. 程序计算均值、样本标准差、A/B 类及合成不确定度，修约后给出最终结果
5. 可选输入单位（仅附加在结果中展示，不做换算）
6. 询问是否保存报告：输入文件名保存到 `reports/`，回车则使用带时间戳的默认名

### 间接测量法

1. 输入公式，如 `g = 4*pi^2*L/T^2`（等号左侧为结果符号，可省略）
2. 选择所有变量统一的输入方式：原始数据（每个变量多次测量 + 仪器误差 + 单位）或直接输入 均值/不确定度/单位
3. 可选输入期望输出单位（回车使用推导单位，如 `m/s^2`）
4. 报告展示偏导、传递项、教材捷径、修约全过程，最终结果如 `g = (9.9 ± 0.2)×10² cm/s^2`
5. 询问是否保存报告（同直接测量法）

示例输出：

```
最终结果: g = (9.9 ± 0.2)×10² cm/s^2
```

### 作图与线性拟合

1. 选择模式 3，分两行输入 x、y 数据（如 `15.0 25.8 30.0 36.6 44.4` 和 `39.4 42.9 44.4 46.6 49.2`，个数须一致）
2. 输入横轴/纵轴物理量名称与单位（如 `T` / `℃`、`R` / `Ω`）和图名（如 `铜丝R-T曲线`）
3. 程序用最小二乘法拟合并绘图，保存到 `figures/`（文件名可自定义，默认时间戳）
4. 选择求斜率截距的方法：`1) 图解法`（自动在直线两端取点，两点式求斜率，并重绘一张标记取点图）或 `2) 线性回归法`（直接给出 a、b、R 与回归方程）
5. 报告展示数据表、Lxy/Lxx/Lyy 等中间量与所选方法过程，询问是否保存到 `reports/`

示例输出片段：

```
斜率     a = Lxy / Lxx          = 0.334258 Ω/℃
截距     b = y_bar - a*x_bar    = 34.3519 Ω
相关系数 R = Lxy / sqrt(Lxx*Lyy) = 0.999931
回归方程: R = 0.334258*T + 34.3519 Ω
```

> 注：程序保留全部精度，教材算例中间量取 3 位有效数字（得 a=0.32、b=34.8、R=0.95），两者都是正确结果，差异仅源于中间量舍入。

## 项目结构与基本设置

```
physics_lab/
├── main.py                  # 程序入口，模式选择与流程编排
├── pyproject.toml           # 打包配置（名称、版本、Python 版本要求、依赖）
├── requirements.txt         # 运行依赖
├── readme.md
├── docs/
│   ├── fixes_and_design.md      # 错误修复对照与直接测量法设计说明
│   ├── indirect_measurement.md  # 间接测量法设计说明、常数表与使用文档
│   ├── 绘图与线性回归总结.md      # 教材图示法/图解法/最小二乘法要求总结
│   └── graph_and_fit_design.md  # 作图与线性拟合模块设计说明（v0.3.0）
├── tests/                   # pytest 测试（解析、误差传递、修约、单位、回归）
└── src/physics_lab/
    ├── __init__.py          # 版本号与公共 API 导出
    ├── method.py            # 均值、样本标准差
    ├── outlier.py           # Grubbs 检验
    ├── uncertainty.py       # A/B 类不确定度与合成
    ├── result.py            # 有效数字修约、科学记数法与结果格式化
    ├── constants.py         # 数学/物理常数表
    ├── units.py             # pint 单位解析、量纲推导与换算
    ├── formula.py           # 公式解析（sympy）、偏导、幂单项式检测
    ├── propagation.py       # 误差传递（一般式 + 教材捷径）
    ├── indirect.py          # 间接测量法交互编排
    ├── regression.py        # 最小二乘拟合、图解法（计算内核）
    ├── plotting.py          # matplotlib 绘图与中文字体检测
    ├── graph.py             # 作图与线性拟合交互编排
    └── io.py                # 输入输出（交互、报告打印与保存）
```

`figures/`（拟合图）与 `reports/`（报告）为运行生成的输出目录，均不入库。

### 常用设置及修改方法

| 设置项 | 位置 | 默认值 | 说明 |
| --- | --- | --- | --- |
| Grubbs 检验显著性水平 | `main.py` 顶部 `ALPHA` | `0.05` | 也可单独调用 `grubbs_test(data, alpha=...)` 指定 |
| 报告保存文件夹 | `src/physics_lab/io.py` 顶部 `REPORT_DIR` | `reports/`（相对运行目录） | 修改为绝对路径可固定保存位置 |
| 不确定度修约规则 | `src/physics_lab/result.py` | `ROUND_CEILING`（1 位有效数字，只进不舍） | 换用其他 `ROUND_*` 常量可改变进位方式 |
| 测量值对齐修约 | `src/physics_lab/result.py` | `ROUND_HALF_EVEN`（四舍六入五成双，GB/T 8170） | 换成 `ROUND_HALF_UP` 即普通四舍五入 |
| B 类不确定度分布假设 | `src/physics_lab/uncertainty.py` | 均匀分布 `Δ / √3` | 若按正态分布可取 `Δ / 3` |
| 内置常数 | `src/physics_lab/constants.py` | CODATA 2018 常数集 | 增删 `CONSTANTS` 字典即可 |
| 拟合图保存文件夹 | `src/physics_lab/plotting.py` 顶部 `FIGURES_DIR` | `figures/`（相对运行目录） | 修改为绝对路径可固定保存位置 |
| Python 版本要求 | `pyproject.toml` | `>=3.10` | 按需调整 |

## 运行测试

```bash
.venv/bin/python -m pytest tests/ -q
```

## 常见问题

- **为什么我的某个数据没被 Grubbs 剔除？** Grubbs 准则基于显著性水平与样本量判断，两个相同的可疑极值会产生"掩蔽效应"导致判不出，属统计方法固有局限。
- **为什么 Grubbs 临界值与 NIST 等国际资料对不上？** 程序采用国内《大学物理实验》教材的格拉布斯系数表（单侧公式，t 分布显著性水平为 alpha/n），如 n=10、α=0.05 时临界值为 2.18；NIST 等资料使用双侧公式 alpha/(2n)，对应 2.29。两种形式在文献中都存在，本项目以教材对照表为准。
- **报告文件在哪？** 默认保存在运行目录下的 `reports/` 文件夹，保存后程序会打印完整路径。
- **公式里的 `h` 为什么变成了普朗克常数？** 与内置常数同名的符号一律识别为常数（报告顶部会列出提示），测量变量请换名（如高度用 `H`）。
- **`T2` 为什么算成 `T*2`？** 隐式乘法会把数字后缀拆开，下标风格请写 `T_2`。
- **温度可以用 ℃ 吗？** ℃ 是偏移量单位，不能参与乘除，请用 `delta_degC`（温差）或开尔文 `K`。
- **拟合图在哪里？** 保存到运行目录下的 `figures/` 文件夹；图解法选择的取点图会额外保存一张标记 A、B、C 三点的图。
- **图中中文显示为方框？** 程序会自动检测系统中文字体（如 Noto Sans CJK），若提示"未检测到中文字体"，请安装中文字体或将图名、标签改为英文。
- **程序算出的斜率和教材答案不一样？** 教材算例把中间量（Σxy、Σx² 等）取 3 位有效数字后再计算，程序保留全部精度。例如同组数据教材得 a=0.32、b=34.8、R=0.95，程序得 a≈0.334、b≈34.35、R≈0.9999，两者都是正确的最小二乘结果。
